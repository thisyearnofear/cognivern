# Two-package layout:
#   .        → cognivern-auction         (templates only; this is what gets
#              uploaded to the Canton participant — no daml-script dependency)
#   ./scripts → cognivern-auction-scripts (setup + tests; local `daml test`
#              and sandbox seeding only, never uploaded)
packages:
  - .
  - ./scripts
