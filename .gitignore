# ── Dependencies ─────────────────────────────────────────────────────
node_modules/
.pnp
.pnp.js

# ── Build outputs ───────────────────────────────────────────────────
dist/
build/
out/
.next/
*.tsbuildinfo
src/frontend/dist/

# ── Daml build artifacts (DARs are rebuilt from source; never commit them) ──
*.dar
**/.daml/

# ── TypeScript compiled output in source tree ───────────────────────
src/backend/**/*.js
src/backend/**/*.js.map
src/backend/**/*.d.ts
src/backend/**/*.d.ts.map
!src/backend/types/openai.d.ts

# ── Logs ────────────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# ── Runtime data ────────────────────────────────────────────────────
data/
.cognivern/

# ── Environment variables & secrets ─────────────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.vercel
!.env.example
*.pem
*.key

# ── Testing ─────────────────────────────────────────────────────────
coverage/
*.lcov
.nyc_output
playwright-report/
test-results/
.testsprite/runs/

# ── IDE / Editor ────────────────────────────────────────────────────
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# ── OS ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── AI / IDE assistant artifacts ────────────────────────────────────
.aider*
.artifacts/
.blackbox/
.blackboxcli/
.claude/
.openclaude/
.commandcode/
.cursor/
.junie/
.letta/
.qwen/
.windsurf/
.zeav/
.zencoder/
.zenflow/
.kimchi

# ── pnpm store ─────────────────────────────────────────────────────
.pnpm/

# ── Deploy artifacts ───────────────────────────────────────────────
deploy-bundle/
.vercel/

# ── Video / media / binary assets (host externally, don't bloat git) ──
videos/
docs/demo-video-canton/
*.mp4
*.webm
*.mov
*.avi
*.mkv
*.mp3
*.wav
*.aiff
*.ogg
*.flac
docs/*.pptx
docs/*.pdf

# ── TestSprite transient output ────────────────────────────────────
src/frontend/testsprite_tests/tmp/

# ── Generated plots / images ───────────────────────────────────────
scripts/hackathon/*.png

# ── Tool version managers ──────────────────────────────────────────
.volta/
.tool-versions
.mise/

# ── Wrangler ───────────────────────────────────────────────────────
.wrangler/
.dev.vars

# ── Debug ───────────────────────────────────────────────────────────
.vite-inspect

# ── Pre-commit ─────────────────────────────────────────────────────
.secrets.baseline

# ── Private operations docs ─────────────────────────────────────────
docs/OPS.md

# ── Root-level overrides (use config/ versions) ────────────────────
/ecosystem.config.cjs

# ── Deploy scripts with server-specific values ─────────────────────
scripts/deploy.sh
scripts/monitoring/monitor-api.sh

# ── External repos ─────────────────────────────────────────────────
remix-smart-bin-monitoring-das/
.qoder/

# ── Root-level tool artifacts (pnpm/tsc workspace side-effects) ────
/tsc
/cognivern@*
/cognivern-frontend@*
BRIEF.md
