import { Agent, AgentMetrics } from '../../types/Agent.js';

// Agent entity and value object definitions can be placed here.
// For now, we re-export the type for clarity.
export type { Agent, AgentMetrics };
