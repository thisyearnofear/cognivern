/**
 * @deprecated Import from `../shared/logging/Logger.js` directly instead.
 * This re-exports the unified logger for backward compatibility.
 */
export { logger as default } from "@backend/shared/logging/Logger.js";
